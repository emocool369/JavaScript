function roadRoad(speed, area){
    switch(area){
        case "motorway": break;
        let diff = Math.abs(speed - 130);
        case "interstate": break;
        case "city": break;
        case "residential": break;
    }
}